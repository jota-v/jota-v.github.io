import { css } from 'glamor';

let Styles = {
  'hero': css({
    minHeight: '36em',
    textAlign: 'center',
    backgroundColor: '#666',
    backgroundImage: 'url("../images/build-bg.jpg")',
    backgroundSize: 'cover',
    backgroundRepeat: 'no-repeat',
    backgroundPosition: 'center',
  })
};

module.exports = Styles;
