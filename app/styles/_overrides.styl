.owl-theme .owl-nav.disabled + .owl-dots {
  margin-top: 0;
}

.owl-carousel{
  position: relative;
}

.owl-theme .owl-nav.disabled + .owl-dots {
  position: absolute;
  bottom: 0;
  width: 100%;
  background: rgba(0,0,0,.1)
}

.owl-carousel .owl-item img.waykup {
  width: 68%;
  margin: 0 auto;
}
.owl-carousel .owl-item img.meli {
  position: relative;
  width: 112%;
  right: -5%;
}

.owl-carousel .owl-item img.trivia {
  position: relative;
  width: 100%;
  left: -5%;
}
